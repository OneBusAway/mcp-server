# Local development only. Copy to .env and keep the real file out of Git.
OBA_API_KEY=test

# Required only when OBA_TRANSPORT=streamable-http.
# OBA_HTTP_AUTH_TOKEN=replace-with-a-local-token

# Optional overrides for config.json or compiled defaults.
# OBA_BASE_URL=http://localhost:4000
# OBA_TRANSPORT=stdio
# OBA_TOOL_PROFILE=all
# OBA_HTTP_BIND_ADDR=127.0.0.1
# OBA_PORT=8080
# OBA_ALLOWED_ORIGINS=http://localhost:5173
# OBA_LOG=/tmp/oba-mcp.log
# OBA_LOG_JSON=0
# OBA_LOG_FORMAT=text
# OBA_CACHE=/path/to/cache.db
